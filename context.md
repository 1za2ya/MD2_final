# context.md 仕様変更部分

# 0. 最重要仕様

本プロジェクトでは、以下の技術・機能を必須とする。

これらは任意機能ではなく、プロジェクトの中心となる要件である。AIや開発者は、別の技術への置き換えや省略を行わないこと。

1. **A-Frameを用いて3D Web空間を作成する**
2. **WebXRに対応したブラウザ上の3D空間として実装する**
3. **Node.jsとSocket.IOを用いてマルチプレイ化する**
4. **複数ユーザーが同じ3D空間を共有できるようにする**
5. **各ユーザーの位置、移動、コイン取得などの行動ログを記録する**
6. **記録したデータをCSV形式で出力する**
7. **CSVデータをPythonとpandasで分析する**
8. **matplotlibを使用して分析結果を可視化する**
9. **ゲーム内から取得できる客観的なデータだけを分析に使用する**
10. **アンケートなどの主観的なデータ収集は行わない**

---

# 1. プロジェクト概要

本プロジェクトでは、A-Frameを使用して、ブラウザ上で動作する3D迷路型コイン収集ゲームを開発する。

A-Frameによって作成した3D空間はWebXRに対応させ、通常のパソコン用ブラウザだけでなく、対応環境ではVR機器からもアクセスできる構成を目指す。

プレイヤーは3D迷路内を移動し、迷路内に配置されたすべてのコインを収集する。

ゲームは、以下の2種類のモードを実装する。

- 1人でプレイする個人プレイ
- 複数ユーザーが同じ空間に参加する競争プレイ

競争プレイでは、Node.jsとSocket.IOを使用してリアルタイム通信を行う。

複数のプレイヤーが同じゲームルームへ参加し、同じ3D迷路空間を共有する。各プレイヤーの位置、向き、コイン取得数、クリア状況などをSocket.IOによって同期する。

ゲーム内では、プレイヤーの移動経路、クリア時間、移動距離、コイン取得順序などの行動ログを自動的に記録する。

記録したデータはSQLite3へ保存し、分析用のCSVファイルとして出力する。

出力したCSVデータはPythonのpandasを使用して集計・分析し、matplotlibによってグラフや移動経路図として可視化する。

---

# 2. 研究テーマ

## メインテーマ

**A-Frameを用いた3D Web迷路空間における、プレイヤーの探索行動とコイン収集効率の分析**

## マルチプレイを含めたテーマ

**WebXR対応3D迷路空間において、個人プレイと複数ユーザーによる競争プレイの違いが探索行動に与える影響**

## 最終的に明らかにしたいこと

本研究では、以下の点を明らかにする。

> A-Frameによって作成された3D Web迷路空間において、コインを効率よく収集するプレイヤーは、どのような移動や経路選択を行っているのか。また、Socket.IOによって複数ユーザーが同じ空間を共有した場合、個人プレイと比較して行動がどのように変化するのか。

---

# 3. 研究目的

## 3.1 3D Web空間の開発

A-Frameを使用して、ブラウザ上で動作する3D迷路空間を作成する。

A-Frameのエンティティ・コンポーネント構造を使用し、以下を実装する。

- 迷路の壁
- 床
- プレイヤー視点
- プレイヤー操作
- コイン
- スタート地点
- ゴール条件
- 他プレイヤーのアバター
- ゲーム内UI
- WebXRへの対応

## 3.2 マルチプレイ空間の開発

Node.jsとSocket.IOを使用し、複数ユーザーが同じ3D空間を共有できる仕組みを実装する。

各ユーザーはゲームルームへ参加し、同じ迷路とコイン配置を共有する。

共有する情報は以下とする。

- プレイヤーID
- ルームID
- プレイヤーの位置
- プレイヤーの向き
- プレイヤーの接続状態
- 取得済みコイン数
- 残りコイン数
- ゲーム開始状態
- ゲーム終了状態
- クリア時間
- 現在の順位

## 3.3 行動ログの収集

プレイヤーの行動を一定間隔で記録し、分析可能な形式で保存する。

記録対象には以下を含める。

- プレイヤーの位置
- プレイヤーの向き
- 移動速度
- 移動距離
- 停止時間
- 通過したエリア
- 方向転換
- コイン取得
- 他プレイヤーとの距離
- 順位変化
- ゲーム開始時刻
- ゲーム終了時刻

## 3.4 CSVとpandasによる分析

ゲームから収集したデータをCSV形式で出力し、pandasを用いて分析する。

アンケートは実施せず、ゲームシステムが自動的に記録した客観的な行動データだけを使用する。

---

# 4. 使用技術

本プロジェクトでは、以下の技術を使用する。

## 4.1 3D Web・WebXR

- HTML
- CSS
- JavaScript
- A-Frame
- WebXR

### A-Frameの役割

A-Frameは、ブラウザ上に3D空間を構築するために使用する。

以下の要素はA-Frameのエンティティとして実装する。

- 迷路
- 壁
- 床
- コイン
- プレイヤーカメラ
- 他プレイヤーのアバター
- ゴール表示
- 3D空間内の案内表示

A-Frameを使用せず、独自のCanvas 2Dゲームとして実装してはならない。

Three.jsを直接中心技術として使用するのではなく、基本的にはA-Frameの仕組みを通して3D空間を構築する。

A-Frame内部でThree.jsの機能を補助的に使用することは可能だが、その場合もA-Frameを中心とした構成を維持する。

### WebXRの役割

A-FrameのWebXR対応機能を利用して、対応端末ではVRモードへ移行できる構成とする。

ただし、実験をVR機器だけに限定すると参加条件が変わる可能性があるため、基本的な実験は同じ操作環境で実施する。

パソコン操作とVR操作のデータを混在させる場合は、以下の環境情報を記録する。

- `desktop`
- `vr`

操作環境が異なるデータは、原則として分けて分析する。

## 4.2 フロントエンド

- HTML
- CSS
- JavaScript
- A-Frame
- Socket.IO Client

フロントエンドは以下を担当する。

- A-Frameシーンの表示
- 迷路の生成
- プレイヤー操作
- コインの表示と取得判定
- 他プレイヤーの表示
- ゲーム時間の表示
- 残りコイン数の表示
- Socket.IOによるリアルタイム通信
- 行動ログの一時保存
- 行動ログのサーバー送信

## 4.3 バックエンド

- Node.js
- Express
- Socket.IO

### Expressの役割

Expressは以下を担当する。

- HTML、CSS、JavaScriptの配信
- A-Frameゲーム画面の配信
- ゲームセッションの作成
- 行動ログの受信
- SQLite3への保存
- CSVファイルの出力
- セッション結果の取得

### Socket.IOの役割

Socket.IOは以下を担当する。

- ゲームルームの作成
- ゲームルームへの参加
- プレイヤー接続状態の管理
- ゲーム開始時刻の同期
- プレイヤー位置の共有
- プレイヤーの向きの共有
- コイン取得状況の共有
- 順位の更新
- ゲーム終了状態の共有
- 通信切断と再接続の管理

## 4.4 データ保存

- SQLite3
- CSV

ゲーム中の行動ログは、一度SQLite3へ保存する。

その後、pandasで分析しやすいCSV形式として出力する。

## 4.5 データ分析

- Python
- pandas
- matplotlib

pandasを中心として以下を行う。

- CSVの読み込み
- データ型の確認
- 欠損値の確認
- 異常値の確認
- セッション単位の集計
- プレイヤー単位の集計
- ゲームモード間の比較
- 速いプレイヤーと遅いプレイヤーの比較
- 試行回数による変化の分析
- 各指標とクリア時間の相関分析

matplotlibを使用して以下を可視化する。

- 棒グラフ
- 折れ線グラフ
- 散布図
- 箱ひげ図
- 移動経路図
- 通過地点のヒートマップ

---

# 5. システム構成

本プロジェクトは、以下の構成とする。

```text
プレイヤーのブラウザ
    │
    ├─ A-Frameによる3D迷路空間
    ├─ JavaScriptによる操作・当たり判定
    ├─ Socket.IO Clientによるリアルタイム通信
    └─ 行動ログの一時保存
            │
            ▼
Node.jsサーバー
    │
    ├─ ExpressによるWeb配信・API
    ├─ Socket.IOによる共有空間の同期
    ├─ ゲームルーム管理
    └─ ログの検証・保存
            │
            ▼
SQLite3
    │
    ├─ プレイヤー情報
    ├─ セッション情報
    ├─ 移動ログ
    ├─ コイン取得ログ
    └─ イベントログ
            │
            ▼
CSV出力
    │
    ▼
Python・pandasによる分析
    │
    ▼
matplotlibによる可視化
```

---

# 6. A-Frameによる3D空間の仕様

## 6.1 A-Frameシーン

ゲーム画面にはA-Frameの`a-scene`を使用する。

A-Frameシーン内に以下を配置する。

- カメラ
- プレイヤーリグ
- 迷路の床
- 迷路の壁
- コイン
- 光源
- 他プレイヤーのアバター
- 必要な3D UI

## 6.2 プレイヤー

各プレイヤーは、A-Frame空間内に一意のプレイヤーエンティティを持つ。

ローカルプレイヤーには以下を設定する。

- カメラ
- 移動操作
- 視点操作
- 当たり判定
- コイン取得判定

他プレイヤーは、Socket.IOから受け取った位置と向きをもとに、A-Frame上のアバターとして表示する。

## 6.3 迷路

迷路は、A-Frameのエンティティを組み合わせて作成する。

迷路の壁や床には、分析用のIDを設定する。

例：

- `wall_01`
- `corridor_01`
- `area_01`
- `dead_end_01`

位置座標だけでなくエリアIDも記録することで、再訪問や行き止まり進入を分析しやすくする。

## 6.4 コイン

コインはA-Frameエンティティとして配置する。

各コインには以下の情報を持たせる。

- コインID
- X座標
- Y座標
- Z座標
- 配置グループID
- 取得状態

コインは、取得可能な候補地点から選択して配置する。

壁の内部や到達できない位置へ配置しない。

---

# 7. 複数ユーザーが同じ空間を共有する仕組み

## 7.1 ゲームルーム

プレイヤーは、Socket.IOを通してゲームルームへ参加する。

同じルームIDを持つプレイヤーは、同じ迷路空間を共有する。

サーバーはルームごとに以下の状態を管理する。

- 参加プレイヤー
- 接続状態
- 準備状態
- ゲーム開始時刻
- ゲーム終了状態
- 使用する迷路ID
- コイン配置ID
- 乱数シード
- プレイヤーごとの取得コイン数
- プレイヤーごとのクリア状態
- 現在の順位

## 7.2 空間情報の同期

Socket.IOを使用して、以下を同期する。

- プレイヤー位置
- プレイヤーの向き
- プレイヤーの接続・切断
- ゲーム開始
- コイン取得
- 取得コイン数
- クリア
- 順位

位置情報は毎フレーム送信しない。

毎フレーム送信すると通信量が増加し、サーバーやクライアントの負荷が高くなるため、一定間隔で送信する。

表示上の動きを滑らかにするため、他プレイヤーのアバターは受信した位置へ瞬間移動させるのではなく、必要に応じて補間する。

## 7.3 サーバーを基準とした管理

ゲーム開始時刻やプレイヤーの接続状態など、複数ユーザー間で共通にする必要がある情報は、サーバーを基準として管理する。

各ブラウザの時刻だけを使用すると端末ごとの差が生じる可能性があるため、競争プレイの開始時刻はSocket.IOサーバーから通知する。

## 7.4 他プレイヤーの表示

他プレイヤーは、A-Frame空間内にアバターとして表示する。

アバターの表示に最低限必要な情報は以下とする。

- プレイヤーID
- X座標
- Y座標
- Z座標
- Y軸方向の回転
- 接続状態

---

# 8. ゲームモードと連続プレイ

本プロジェクトでは、以下の2種類のゲームモードを実装する。

- ソロプレイ
- マルチプレイ

どちらのモードでも、1回クリアするたびに最初の入力画面へ戻るのではなく、同じ設定を引き継いで次のゲームへ移行できるようにする。

ただし、分析上は1回ごとのプレイを区別する必要があるため、次のゲームへ移行するときは新しいセッションIDを発行し、試行回数を1増やす。

---

## 8.1 ソロプレイ

ソロプレイでは、1人で3D迷路へ参加し、すべてのコインを収集する。

他プレイヤーの位置、取得状況、順位などは表示しない。

ソロプレイをクリアした後は、プレイヤーIDやゲームモードなどを入力する最初の画面へ戻らず、そのまま次のゲームへ移行できるようにする。

### ソロプレイの基本的な流れ

1. プレイヤーが入力画面で必要な情報を入力する
2. ソロプレイを開始する
3. プレイヤーがすべてのコインを取得する
4. クリア結果を表示する
5. 現在のプレイデータを保存する
6. 新しいセッションIDを発行する
7. 試行回数を1増やす
8. 迷路とコインを次のゲーム用にリセットする
9. 入力画面を介さずに次のゲームを開始する

最初に入力した以下の情報は、連続プレイ中は引き継ぐ。

- プレイヤーID
- ゲームモード
- 操作環境
- 使用する迷路の設定
- その他、実験条件として固定する設定

一方、以下の情報はゲームごとに新しく設定または初期化する。

- セッションID
- 試行回数
- 開始時刻
- 終了時刻
- 経過時間
- 取得済みコイン
- 移動ログ
- コイン取得ログ
- イベントログ
- 総移動距離
- 停止時間
- 方向転換回数
- 再訪問回数
- 行き止まり進入回数
- クリア状態

---

## 8.2 ソロプレイのクリア後画面

ソロプレイをクリアした後は、短い結果画面を表示する。

結果画面には、最低限以下を表示する。

- クリア時間
- 総移動距離
- 試行回数
- 取得したコイン数
- 次のゲームへ進むことを示す表示

結果画面の後は、プレイヤーIDやモードを入力する画面へ戻さず、自動的に次のゲームへ移行する。

必要に応じて、次のゲーム開始前に短いカウントダウンを表示する。

例：

```text
3
2
1
START
```

連続プレイを終了するための「終了」ボタンは用意する。

プレイヤーが終了を選択した場合は、未送信ログをサーバーへ送信してから、最初の画面または終了画面へ戻る。

---

## 8.3 マルチプレイ

マルチプレイでは、複数ユーザーがSocket.IOの同一ルームへ参加し、同じA-Frame空間を共有する。

各プレイヤーは、同じ迷路、同じ開始条件、同じコイン配置でゲームを行う。

他プレイヤーは、A-Frame空間内にアバターとして表示する。

マルチプレイでは、以下の情報を共有する。

- プレイヤーの位置
- プレイヤーの向き
- 取得済みコイン数
- 現在の順位
- クリア状態
- 接続状態
- 次のゲームへの準備状態

---

## 8.4 マルチプレイにおけるコインの扱い

マルチプレイでは、各プレイヤーが自分専用のコイン取得状態を持つ独立型を採用する。

他プレイヤーがコインを取得しても、自分の画面上にある同じコインは消えない。

この方式を採用する理由は、すべてのプレイヤーが同じ数、同じ配置のコインを収集し、同一条件でクリア時間や移動経路を比較できるようにするためである。

同じルーム内では、以下の条件を共通にする。

- 迷路ID
- コイン配置ID
- 乱数シード
- 総コイン数
- 開始位置
- ゲーム開始時刻
- 制限時間
- プレイヤーの最大移動速度

---

## 8.5 マルチプレイのクリア後

マルチプレイでは、1回のゲームが終了してもルームを解散しない。

同じメンバーのまま、同じルーム内で次のゲームへ移行できるようにする。

次のゲームへ移行するために、以下の状態を維持する。

- ルームID
- 参加プレイヤー一覧
- Socket.IOのルーム接続
- プレイヤーID
- 操作環境
- ゲームモード
- ルーム内で使用する基本設定

一方で、プレイごとのデータを区別するため、以下は次のゲーム開始時に新しくする。

- セッションID
- ルーム内のラウンド番号
- プレイヤーごとの試行回数
- ゲーム開始時刻
- ゲーム終了時刻
- 移動ログ
- コイン取得ログ
- イベントログ
- 取得済みコイン
- クリア状態
- 順位
- 各種分析指標

---

## 8.6 マルチプレイのラウンド管理

ルームを解散せずに複数回ゲームを行うため、マルチプレイではルームごとにラウンド番号を管理する。

例：

```text
room_id: room_001
round_number: 1
```

1回目のゲームが終了し、次のゲームへ進む場合は以下のように更新する。

```text
room_id: room_001
round_number: 2
```

ルームIDは同じまま維持し、ラウンド番号だけを増やす。

これにより、同じメンバーで行った複数回のゲームを、同じルーム内の連続した実験として分析できる。

CSVおよびSQLite3には、マルチプレイの連続ゲームを区別するため、`round_number`を記録する。

---

## 8.7 マルチプレイの1ゲーム終了条件

マルチプレイでは、以下のいずれかを1ゲームの終了条件とする。

### 基本方式

ルーム内のすべてのプレイヤーがクリアした時点で、そのゲームを終了する。

### 制限時間を使用する場合

以下のいずれかが成立した時点でゲームを終了する。

- ルーム内の全プレイヤーがクリアした
- 設定された制限時間を超えた
- 続行可能なプレイヤーがいなくなった

先にクリアしたプレイヤーは、他のプレイヤーが終了するまで結果画面で待機する。

待機中にゲーム空間へ戻って移動できるようにすると、他プレイヤーの行動へ影響を与える可能性があるため、クリアしたプレイヤーの操作は停止する。

ただし、他プレイヤーの進行状況や現在の順位は確認できるようにしてもよい。

---

## 8.8 マルチプレイの次ゲームへの移行

1回のゲームが終了した後は、同じルーム内に結果画面を表示する。

結果画面には、最低限以下を表示する。

- 各プレイヤーの順位
- 各プレイヤーのクリア時間
- 各プレイヤーの取得コイン数
- 現在のラウンド番号
- 次のゲームへの準備状態
- ルームから退出するためのボタン

各プレイヤーは、「次のゲームへ進む」ボタンを押して準備完了状態になる。

全プレイヤーが準備完了になった場合、ルームを解散せずに次のゲームを開始する。

### 次ゲーム開始までの流れ

1. 現在のゲームを終了する
2. 各プレイヤーのログを保存する
3. 結果画面を表示する
4. 各プレイヤーが次ゲームへの準備を完了する
5. サーバーが全プレイヤーの準備状態を確認する
6. ラウンド番号を1増やす
7. 各プレイヤーへ新しいセッションIDを発行する
8. 次ゲーム用の迷路とコイン状態を初期化する
9. サーバー基準のカウントダウンを開始する
10. 同じルームのまま次のゲームを開始する

---

## 8.9 準備完了状態

マルチプレイのプレイヤー状態には、次ゲームへ進む準備ができているかを表す値を持たせる。

例：

```javascript
{
  playerId: "player_001",
  isConnected: true,
  isCleared: true,
  isReadyForNextRound: false
}
```

プレイヤーが「次のゲームへ進む」を選択した場合、Socket.IOを使用して準備完了状態をサーバーへ送信する。

```javascript
socket.emit("ready_for_next_round", {
  roomId,
  playerId
});
```

サーバーは、ルーム内の接続中プレイヤーが全員準備完了になったかを確認する。

```javascript
// ルームを作り直さず同じ参加者で次のゲームを開始できるように、
// 接続中の全プレイヤーが準備完了した時点で次ラウンドへ移行する。
const allPlayersReady = room.players
  .filter((player) => player.isConnected)
  .every((player) => player.isReadyForNextRound);
```

全員が準備完了になった場合、サーバーは次のラウンド開始を通知する。

---

## 8.10 次ゲーム開始時のリセット処理

次のゲームへ移行する際は、前回のゲームデータが混ざらないように、以下を必ずリセットする。

- 経過時間
- タイマー表示
- プレイヤー位置
- プレイヤーの向き
- 取得済みコインID
- 取得コイン数
- 残りコイン数
- クリア状態
- 現在の順位
- 移動ログの一時バッファ
- コイン取得ログの一時バッファ
- イベントログの一時バッファ
- 総移動距離
- 停止時間
- 方向転換回数
- 再訪問回数
- 行き止まり進入回数
- ゲーム開始時刻
- ゲーム終了時刻
- 次ゲームへの準備状態

前回のゲームデータはリセット前にSQLite3へ保存し、未送信ログがないことを確認する。

---

## 8.11 次ゲームの迷路とコイン配置

次のゲームで同じ迷路と同じコイン配置を使用するか、異なる配置を使用するかは、実験条件として事前に決定する。

### 同じ配置を使用する場合

同じ乱数シードとコイン配置IDを使用する。

この方式は、プレイを繰り返すことで移動経路やクリア時間がどのように変化するかを分析する場合に使用する。

### 異なる配置を使用する場合

新しい乱数シードとコイン配置IDを発行する。

この方式は、特定の配置を記憶したことによる影響を小さくし、異なる配置への対応を分析する場合に使用する。

マルチプレイでは、次のゲームでもルーム内の全プレイヤーに同じ乱数シードを配布する。

---

## 8.12 プレイヤーの退出

マルチプレイの結果画面では、プレイヤーがルームから退出できるようにする。

プレイヤーが退出した場合は、以下を行う。

1. 未送信ログをサーバーへ送信する
2. プレイヤーのセッションを終了する
3. Socket.IOのルームから退出する
4. 他プレイヤーへ退出を通知する
5. A-Frame空間から退出したプレイヤーのアバターを削除する

一部のプレイヤーが退出しても、残りのプレイヤーが2人以上いる場合は、同じルームで次のゲームを続けられるようにする。

残りのプレイヤーがマルチプレイの最低人数を下回った場合は、次のゲームを開始せず、ルームの状態を終了または待機状態へ変更する。

---

## 8.13 通信切断時の扱い

結果画面または次ゲーム準備中に通信が切断された場合、すぐにルーム全体を解散しない。

一定時間は再接続を待ち、再接続できた場合は、同じルームへ復帰できるようにする。

再接続できなかった場合は、そのプレイヤーを次のラウンドの参加対象から除外する。

ゲーム中に通信が切断された場合は、切断時点までの行動ログを可能な範囲で保存する。

切断したプレイヤーのセッションには、終了理由として通信切断を記録する。

---

## 8.14 行動ログ上の扱い

連続してゲームを行う場合でも、複数回分のログを1つのセッションへまとめてはならない。

1回のゲームごとに、別のセッションIDを使用する。

例：

```text
room_id: room_001
round_number: 1
session_id: session_001
player_id: player_001
```

次のゲームでは以下のようにする。

```text
room_id: room_001
round_number: 2
session_id: session_002
player_id: player_001
```

この設計によって、同じプレイヤーの1回目、2回目、3回目の移動経路やクリア時間を比較できる。

---

## 8.15 セッションデータへの追加項目

連続プレイを管理するため、セッションデータには以下の項目を含める。

- `session_id`
- `player_id`
- `room_id`
- `game_mode`
- `trial_number`
- `round_number`
- `previous_session_id`
- `started_at`
- `ended_at`
- `is_cleared`
- `end_reason`

`previous_session_id`には、同じプレイヤーが直前にプレイしたセッションIDを記録する。

ソロプレイでは、同じプレイヤーの連続した試行を関連付けるために使用する。

マルチプレイでは、同じルーム内での前回ラウンドと次回ラウンドを関連付けるために使用する。

最初のプレイでは、`previous_session_id`を空欄または`NULL`とする。

---

## 8.16 CSVへの反映

`sessions.csv`には、連続プレイを分析できるように以下の列を追加する。

```csv
session_id,previous_session_id,player_id,room_id,game_mode,trial_number,round_number,device_mode,maze_id,coin_layout_id,random_seed,started_at,ended_at,clear_time,total_distance,average_speed,maximum_speed,stop_time,turn_count,revisit_count,dead_end_count,route_efficiency,collected_coins,total_coins,final_rank,is_cleared,end_reason
```

ソロプレイでは、`room_id`と`round_number`を空欄または`NULL`としてもよい。

ただし、ソロプレイの連続回数は`trial_number`で管理する。

マルチプレイでは、同じルームに参加したプレイヤーについて、同じゲームの`round_number`を共通にする。

---

## 8.17 Socket.IOイベントへの追加

連続したマルチプレイを実現するため、以下のSocket.IOイベントを追加する。

### クライアントからサーバーへ送信するイベント

- `ready_for_next_round`
- `cancel_next_round`
- `leave_room_after_game`

### サーバーからクライアントへ送信するイベント

- `player_ready_for_next_round`
- `next_round_waiting`
- `next_round_countdown`
- `next_round_started`
- `player_left_room`
- `room_closed`

### next_round_started

次のゲーム開始時に、サーバーから以下の情報を通知する。

```javascript
{
  roomId,
  roundNumber,
  sessionId,
  mazeId,
  coinLayoutId,
  randomSeed,
  startedAt
}
```

各プレイヤーのセッションIDは異なるため、実装時にはプレイヤーごとに個別のセッションIDを通知する。

---

## 8.18 AIコーディング支援への追加指示

AIは、ゲームモードと連続プレイを実装する場合、以下の仕様を変更または省略してはならない。

### ソロプレイ

- クリア後に最初の入力画面へ戻さない
- プレイヤー情報とモードを引き継ぐ
- クリア結果を保存してから次のゲームへ移行する
- 次のゲームでは新しいセッションIDを発行する
- 試行回数を1増やす
- 前回の移動ログや取得状態を完全にリセットする
- プレイヤーが連続プレイを終了できる機能を用意する

### マルチプレイ

- 1ゲーム終了後もSocket.IOのルームを解散しない
- 同じメンバーのまま次のゲームへ進める
- ルームIDを維持する
- ラウンド番号を1増やす
- 各プレイヤーに新しいセッションIDを発行する
- 全プレイヤーの準備状態をサーバー側で管理する
- 全員が準備完了してから次のゲームを開始する
- 次のゲーム開始時刻はサーバーを基準に同期する
- 前回のログが次のゲームへ混ざらないようにする
- プレイヤーが結果画面から退出できるようにする
- 一部のプレイヤーが退出しても、条件を満たせばルームを継続する

---

## 8.19 コメントルールへの追加

連続プレイに関係するコードには、以下の設計理由が分かるコメントを追加する。

- なぜソロプレイで入力画面へ戻らないのか
- なぜプレイヤー情報を引き継ぐのか
- なぜゲームごとに新しいセッションIDを発行するのか
- なぜ前回のログを保存してから初期化するのか
- なぜマルチプレイのルームを維持するのか
- なぜルームIDとラウンド番号を分けて管理するのか
- なぜ全プレイヤーの準備完了を待つのか
- なぜ次ゲーム開始時刻をサーバー基準にするのか
- なぜ切断時にすぐルームを解散しないのか
- なぜ前回のゲーム状態を完全にリセットするのか

必要なコメントの例：

```javascript
// 同じプレイヤーの繰り返しによる経路変化を分析できるように、
// 入力情報は引き継ぎながら、プレイごとに新しいセッションIDを発行する。
const nextSessionId = createSessionId();
```

```javascript
// 同じ参加者で複数回の競争データを収集できるように、
// ゲーム終了後もSocket.IOのルームは維持し、ラウンド番号だけを更新する。
room.roundNumber += 1;
```

```javascript
// 前回の座標ログが次のゲームへ混ざると経路分析が正しく行えないため、
// 保存完了を確認してからプレイヤーごとのログバッファを初期化する。
movementLogBuffer.length = 0;
```

一方、以下のようなコードを見れば明らかな処理には、冗長性防止のためコメントを追加しない。

```javascript
trialNumber += 1;
```

ただし、試行回数を増やす研究上の目的が周囲のコードから分からない場合は、処理全体の直前に目的を説明するコメントを記載する。

---

# 9. 行動ログの記録

## 9.1 行動ログを記録する目的

行動ログは、クリア時間だけでは分からないプレイヤーの探索方法を分析するために記録する。

これによって以下を調査する。

- 無駄な移動が多いか
- 同じ場所へ戻っているか
- どこで停止しているか
- どの順番でコインを取得したか
- 競争時に移動速度が変化したか
- 他プレイヤーの存在で経路が変化したか

## 9.2 記録間隔

プレイヤー位置は初期設定として0.2秒ごとに記録する。

毎フレーム記録するとデータ量が大きくなり、通信とデータベース書き込みの負荷も増えるため、一定間隔で記録する。

JavaScript側で複数件のログを一時保存し、一定件数に達したらまとめてExpressサーバーへ送信する。

## 9.3 セッションログ

1回のプレイごとに以下を記録する。

- セッションID
- プレイヤーID
- ルームID
- ゲームモード
- 試行回数
- 操作環境
- 迷路ID
- コイン配置ID
- 乱数シード
- 開始時刻
- 終了時刻
- クリア時間
- クリア状態
- 総移動距離
- 平均移動速度
- 最大移動速度
- 停止時間
- 方向転換回数
- 再訪問回数
- 行き止まり進入回数
- 取得コイン数
- 総コイン数
- 最終順位

## 9.4 移動ログ

一定間隔で以下を記録する。

- セッションID
- プレイヤーID
- ルームID
- 経過時間
- X座標
- Y座標
- Z座標
- X軸方向の回転
- Y軸方向の回転
- Z軸方向の回転
- 移動速度
- 現在のエリアID
- 取得コイン数
- 残りコイン数
- 他プレイヤーとの最短距離
- 現在の順位

WebXRではプレイヤーの視点や向きを分析できるように、必要に応じて3軸の回転情報を記録する。

## 9.5 コイン取得ログ

コインを取得するたびに以下を記録する。

- セッションID
- プレイヤーID
- ルームID
- コインID
- コインの座標
- 取得順序
- 取得時刻
- ゲーム開始からの経過時間
- 直前のコインからの移動距離
- 直前のコイン取得からの経過時間
- 取得時点の順位

## 9.6 Socket.IOイベントログ

マルチプレイに関する以下のイベントを記録する。

- ルーム参加
- 準備完了
- ゲーム開始
- 通信切断
- 再接続
- コイン取得
- 順位変化
- 他プレイヤーの追い越し
- クリア
- ルーム退出

---

# 10. CSVファイル

SQLite3に保存したデータは、以下のCSVとして出力する。

## sessions.csv

```csv
session_id,previous_session_id,player_id,room_id,game_mode,trial_number,round_number,device_mode,maze_id,coin_layout_id,random_seed,started_at,ended_at,clear_time,total_distance,average_speed,maximum_speed,stop_time,turn_count,revisit_count,dead_end_count,route_efficiency,collected_coins,total_coins,final_rank,is_cleared,end_reason
```

## movement_logs.csv

```csv
session_id,player_id,room_id,game_mode,elapsed_time,pos_x,pos_y,rotation_y,speed,area_id,collected_coins,remaining_coins,nearest_opponent_distance,current_rank
```

## coin_logs.csv

```csv
session_id,player_id,room_id,coin_id,coin_x,coin_y,coin_z,collected_order,collected_time,elapsed_time,collected_coins,remaining_coins,distance_from_previous_coin,time_from_previous_coin,current_rank
```

## event_logs.csv

```csv
session_id,player_id,room_id,event_type,elapsed_time,pos_x,pos_y,pos_z,detail
```

---

# 11. pandasによる分析

分析はCSVデータを用いて、pandasを中心に行う。

## 必須分析

最低限、以下を分析する。

- プレイヤーごとのクリア時間
- プレイヤーごとの総移動距離
- クリア時間と総移動距離の関係
- クリア時間と停止時間の関係
- クリア時間と再訪問回数の関係
- 速いプレイヤーと遅いプレイヤーの比較
- 個人プレイとマルチプレイの比較
- プレイ回数による変化
- コイン取得順序の違い
- 移動経路の違い
- 競争中の順位変化
- 他プレイヤーとの距離と行動の関係

## 個人プレイとマルチプレイの比較

`game_mode`には以下を使用する。

- `solo`
- `multiplayer`

同じプレイヤーの個人プレイとマルチプレイを比較できるようにする。

比較する

---

# 17. コードコメントに関する必須ルール

本プロジェクトでコードを作成・修正する場合は、コードを読んだ人が、処理内容だけでなく、**なぜその処理や機能を実装しているのか**を理解できるコメントを追加する。

ただし、すべての行にコメントを付ける必要はない。

コードを見れば処理内容が明らかに分かる部分にまでコメントを追加すると、コードが読みにくくなり、コメントと実際の処理が食い違う原因にもなる。

そのため、本プロジェクトでは以下を基本方針とする。

> コードだけでは分かりにくい設計理由、研究上の目的、通信やデータ保存の意図にはコメントを追加し、コードを見れば明らかな処理には冗長性を防ぐためコメントを追加しない。

---

## 17.1 コメントの目的

コメントは、コードを日本語に置き換えて説明するためのものではない。

以下のような、コードだけでは判断しにくい情報を説明するために使用する。

- なぜその処理が必要なのか
- なぜその実装方法を採用したのか
- その処理が研究目的とどのように関係しているのか
- その処理がどのような不具合を防いでいるのか
- なぜその数値や判定条件を使用しているのか
- 処理を削除または変更すると何が起きるのか
- A-Frame、Socket.IO、SQLite3、CSV、pandas間でどのような関係があるのか
- 複数ユーザー間で条件を統一するために何をしているのか
- 分析結果の再現性を維持するために何をしているのか

---

## 17.2 コメントを必ず追加する処理

以下のような、設計上または研究上重要なコードには、処理の目的や採用理由が分かるコメントを追加する。

### A-Frame・WebXRに関する処理

- 独自のA-Frameコンポーネントを登録する処理
- プレイヤーリグとカメラを分けて管理する処理
- プレイヤーの位置や向きを取得する処理
- 壁との衝突判定を行う処理
- コインとの接触を判定する処理
- 他プレイヤーのアバターを生成・削除する処理
- 他プレイヤーの位置を補間して表示する処理
- デスクトップモードとWebXRモードを判別する処理
- VRとデスクトップのデータを区別して保存する処理
- エリアIDや行き止まりIDをA-Frameエンティティに設定する処理

### Socket.IO・マルチプレイに関する処理

- ゲームルームを作成する処理
- プレイヤーを同じルームへ参加させる処理
- サーバーを基準としてゲーム開始時刻を同期する処理
- プレイヤーの位置と向きを送信する処理
- 位置情報を毎フレーム送信しない処理
- 送信間隔を制限する処理
- 他プレイヤーの位置を補間する処理
- 迷路ID、コイン配置ID、乱数シードを共有する処理
- プレイヤーごとに独立したコイン取得状態を管理する処理
- 順位をサーバー側で計算する処理
- 通信切断を処理するコード
- 再接続したプレイヤーの状態を復元する処理
- 不正または異常な位置データを拒否する処理
- ルーム終了後に状態を削除する処理

### 行動ログに関する処理

- セッションIDを生成する処理
- 匿名のプレイヤーIDを使用する処理
- ルームIDを記録する処理
- プレイヤー位置を0.2秒などの一定間隔で記録する処理
- 移動ログをクライアント側に一時保存する処理
- ログを複数件まとめてサーバーへ送信する処理
- ゲーム終了時に未送信ログを送信する処理
- ページを閉じる前にログ送信を試みる処理
- 通信切断時にも可能な範囲でログを残す処理
- 移動距離を計算する処理
- 移動速度を計算する処理
- 停止時間を判定する処理
- 方向転換回数を判定する処理
- エリアへの再訪問を判定する処理
- 行き止まりへの進入を判定する処理
- コイン取得順序を記録する処理
- 他プレイヤーとの距離を計算する処理
- 順位変化を記録する処理
- 未クリアのセッションを保存する処理

### SQLite3・CSVに関する処理

- SQLite3へデータを保存する処理
- トランザクションを使用して複数ログを保存する処理
- SQLのプレースホルダーを使用する処理
- 外部キーを設定する理由
- SQLite3へ保存してからCSVへ出力する処理
- CSVの列順を固定する処理
- CSV出力時にヘッダー名を統一する処理
- 欠損値を空欄または特定の値として出力する処理
- CSV出力時に既存ファイルを上書きするか追記するかを決める処理
- セッション、移動、コイン取得、イベントを別ファイルに分ける処理

### pandas・matplotlibに関する処理

- 分析対象となるCSVを読み込む処理のうち、複数データを結合する部分
- データ型を変換する処理
- 欠損値を処理するコード
- 重複データを除外するコード
- 異常値を除外または別扱いにするコード
- 未クリアデータを特定の分析から除外する処理
- 通信切断が発生したセッションを除外する処理
- デスクトップとVRのデータを分ける処理
- 異なる迷路やコイン配置のデータを分ける処理
- 速いグループと遅いグループを分類する処理
- 中央値を分類基準として使用する処理
- 個人プレイとマルチプレイを比較する処理
- 同じプレイヤー内で条件を比較する処理
- 相関係数を計算する処理
- 経路効率を計算する処理
- ヒートマップ用に座標を区切る処理
- 分析対象からデータを除外するすべての処理

---

## 17.3 良いコメントの例

### A-Frameコンポーネント

```javascript
// プレイヤー移動とログ記録をHTML内へ直接書くと管理しにくくなるため、
// A-Frameコンポーネントとして分離し、各フレームの更新処理を一か所で管理する。
AFRAME.registerComponent("player-movement", {
  tick(time, deltaTime) {
    updatePlayerPosition(deltaTime);
  }
});
```

### Socket.IOによる位置送信

```javascript
// 毎フレーム位置を送信すると通信量とサーバー負荷が大きくなるため、
// 一定間隔で位置を共有し、他プレイヤー側では座標を補間して表示する。
socket.emit("player_position", positionData);
```

### 他プレイヤーの位置補間

```javascript
// 受信した座標へ即座に移動させると通信間隔ごとにアバターが飛んで見えるため、
// 現在位置から受信位置までを補間して滑らかに表示する。
remotePlayer.object3D.position.lerp(targetPosition, interpolationRate);
```

### ゲーム開始時刻の同期

```javascript
// 各ブラウザの時刻だけで開始すると端末ごとに時間差が発生するため、
// サーバーが決定した開始時刻を全プレイヤーへ通知する。
io.to(roomId).emit("game_started", {
  startedAt: serverStartTime
});
```

### 乱数シードの共有

```javascript
// 同じルーム内のプレイヤーが同一のコイン配置で競争できるように、
// サーバーが決定した乱数シードを全クライアントで共有する。
const coinPositions = generateCoinPositions(room.randomSeed);
```

### 独立型コイン

```javascript
// 他プレイヤーがコインを取得しても自分のコインを残すことで、
// 全プレイヤーが同じ数と配置のコインを収集する条件を維持する。
playerState.collectedCoinIds.add(coinId);
```

### 移動ログの一括送信

```javascript
// 位置ログを1件ずつ送信するとAPI呼び出しとDB書き込みが増えるため、
// クライアント側で一定件数を保持し、まとめてサーバーへ送信する。
movementLogBuffer.push(log);

if (movementLogBuffer.length >= MOVEMENT_BATCH_SIZE) {
  sendMovementLogs();
}
```

### 総移動距離の計算

```javascript
// クリア時間だけでは経路の効率を判断できないため、
// 前回記録した座標との差を累積して総移動距離を求める。
totalDistance += previousPosition.distanceTo(currentPosition);
```

### 未クリアデータの保存

```javascript
// 未クリアのプレイも途中までの探索行動を分析できるため、
// 終了理由とともにセッションデータを保存する。
await finishSession({
  isCleared: false,
  endReason: "disconnected"
});
```

### SQLのプレースホルダー

```javascript
// クライアントから受け取った値をSQL文字列へ直接埋め込まず、
// SQLインジェクションと値の解釈ミスを防ぐためプレースホルダーを使用する。
db.run(
  "INSERT INTO players (player_id, created_at) VALUES (?, ?)",
  [playerId, createdAt]
);
```

### pandasでクリア済みデータを抽出

```python
# 未クリアのセッションには通常のクリア時間が存在しないため、
# クリア時間を比較する分析ではクリア済みセッションだけを使用する。
cleared_sessions = sessions[sessions["is_cleared"] == 1].copy()
```

### 中央値によるグループ分け

```python
# 平均値は極端に速い、または遅い記録の影響を受けやすいため、
# クリア時間の中央値を基準に速いグループと遅いグループを分ける。
median_clear_time = cleared_sessions["clear_time"].median()
```

### 操作環境を分ける処理

```python
# デスクトップ操作とVR操作では移動方法や視点操作が異なるため、
# 操作環境の違いが結果へ混ざらないように別々に集計する。
desktop_sessions = sessions[sessions["device_mode"] == "desktop"]
vr_sessions = sessions[sessions["device_mode"] == "vr"]
```

### 異なるコイン配置を分ける処理

```python
# コイン配置が異なると必要な移動距離や難易度も変わるため、
# 同じ配置IDを持つセッション同士で比較する。
layout_groups = sessions.groupby("coin_layout_id")
```

---

## 17.4 不要なコメントの例

コードを見れば処理が明らかな場合は、冗長性を防ぐためコメントを追加しない。

### 変数への単純な代入

```javascript
// コイン数を0にする
let collectedCoins = 0;
```

### 単純な加算

```javascript
// コイン数を1増やす
collectedCoins += 1;
```

### 関数名と同じ説明

```javascript
// ゲームを開始する
startGame();
```

### CSV読み込みの言い換え

```python
# CSVを読み込む
sessions = pd.read_csv("data/exports/sessions.csv")
```

### グラフ表示の言い換え

```python
# グラフを表示する
plt.show()
```

### ループ内容の言い換え

```javascript
// プレイヤーを繰り返す
for (const player of players) {
  updatePlayer(player);
}
```

これらのコメントはコードと同じ内容を言い換えているだけであり、設計理由や新しい情報を提供していないため不要である。

---

## 17.5 コメントを書くか判断する基準

コメントを書くか迷った場合は、以下を確認する。

1. 初めてコードを読む人が、処理の目的を理解できるか
2. なぜ別の方法ではなく、この方法を採用したのか分かるか
3. 研究目的との関係がコードだけで分かるか
4. 通信量やデータ量を減らす目的が分かるか
5. 実験条件を統一する意図が分かるか
6. 数値や判定基準の理由が分かるか
7. 分析対象からデータを除外する理由が分かるか
8. 将来処理を変更する人が、変更時の注意点を理解できるか
9. 処理を削除した場合に発生する問題が分かるか

これらの情報がコードだけでは分かりにくい場合は、コメントを追加する。

反対に、単純な変数代入、一般的な関数呼び出し、明確な条件分岐など、コードそのものから目的を十分に理解できる場合はコメントを省略する。

---

## 17.6 固定値と判定基準のコメント

次のような固定値を使用する場合は、値の意味や採用理由をコメントまたは設定ファイルに記載する。

- 移動ログの記録間隔
- Socket.IOの位置送信間隔
- ログを一括送信する件数
- 停止と判定する速度
- 停止と判定する継続時間
- 方向転換と判定する角度
- 再接続を待つ時間
- プレイヤー位置を補間する割合
- 異常な移動として扱う距離や速度
- ゲームの制限時間

例：

```javascript
// 0.2秒間隔であれば経路分析に必要な精度を保ちながら、
// 毎フレーム記録する場合よりログ量を抑えられる。
const MOVEMENT_LOG_INTERVAL_MS = 200;
```

固定値の理由がまだ決定していない場合は、根拠を推測してコメントに書いてはならない。

その場合は、調整可能な設定値であることを明記する。

```javascript
// 予備実験の結果に応じて調整する暫定的な停止速度の基準。
const STOP_SPEED_THRESHOLD = 0.1;
```

---

## 17.7 関数・クラス・A-Frameコンポーネントの説明

複雑な関数、クラス、A-Frameコンポーネントには、直前に以下を簡潔に記載する。

- 何を担当するか
- なぜ独立した処理として分けているか
- 入力と出力
- 重要な副作用
- 研究データへ与える影響

ただし、関数名と処理内容だけで役割が明らかな小さな関数には、説明を追加しなくてよい。

良い例：

```javascript
/**
 * ローカルプレイヤーの位置を一定間隔で記録する。
 *
 * 毎フレーム記録するとデータ量が過剰になるため、設定された間隔でのみ記録する。
 * 記録したログは一時バッファへ追加され、一定件数ごとにサーバーへ送信される。
 */
function recordMovementLog() {
  // 処理
}
```

冗長な例：

```javascript
/**
 * ゲームを開始する関数。
 */
function startGame() {
  // 処理
}
```

---

## 17.8 TODOコメントのルール

未実装部分に`TODO`コメントを使用する場合は、単に「後で実装する」とだけ書かない。

以下を明記する。

- 未実装の内容
- なぜ必要なのか
- 何が完成すれば対応できるのか
- 現在の処理にどのような制限があるのか

良い例：

```javascript
// TODO: WebXRモードでの移動距離計算を検証する。
// ヘッドセットの細かな頭部移動をプレイヤーの移動として数えると、
// デスクトップ操作との比較条件が変わる可能性があるため。
```

不十分な例：

```javascript
// TODO: 後で直す
```

---

## 17.9 コメントの保守

コードを変更した場合は、関連するコメントも必ず確認する。

以下の状態を残してはならない。

- 実際の処理と異なるコメント
- 使用されていない機能を説明するコメント
- 古い変数名や関数名が書かれたコメント
- 変更前の判定値を説明するコメント
- 実装されていない機能を、実装済みのように説明するコメント
- 実際の理由ではなく推測で書かれたコメント

誤ったコメントは、コメントが存在しない場合よりも誤解を生む可能性がある。

そのため、コードの修正時はコメントもコードの一部として更新する。

---

## 17.10 AIコーディング支援への確認事項

AIはコードを出力または修正する前に、以下を確認する。

- 複雑な処理に、実装理由を説明するコメントがあるか
- A-Frame独自コンポーネントの役割が説明されているか
- Socket.IOの通信間隔や同期方法の理由が説明されているか
- サーバー側で管理する理由が説明されているか
- 行動ログの記録間隔や一括送信の理由が説明されているか
- 研究上重要な条件統一の意図が説明されているか
- 固定値や判定基準の根拠が説明されているか
- エラー処理が、どの問題を防ぐためのものか説明されているか
- pandasでデータを除外・変換する理由が説明されているか
- コードをそのまま日本語にしただけの不要なコメントがないか
- 古くなったコメントが残っていないか

AIはコメントの数を増やすこと自体を目的としてはならない。

コメントは、コードだけでは読み取れない設計意図、研究上の理由、通信や保存の負荷対策、分析条件を補足するために使用する。

---

## 17.11 コメントルールの最終方針

本プロジェクトでは、以下をコメント作成の最終方針とする。

- 「何をしているか」ではなく「なぜしているか」を優先して書く
- 研究目的に関係する処理には理由を記載する
- 通信量やデータ量を抑える処理には目的を記載する
- 複数ユーザーの条件を統一する処理には理由を記載する
- データを除外・変換する分析処理には理由を記載する
- 固定値には根拠または暫定値であることを記載する
- 推測した理由をコメントとして書かない
- コード変更時にはコメントも更新する
- 明らかな処理には冗長性防止のためコメントを書かない
- コメントの量ではなく、コードの理解に必要な情報があるかを重視する

**すべてのコードにコメントを付けるのではなく、コードだけでは分かりにくい「設計理由」と「研究上の意図」にコメントを付けることを必須とする。**

# 18. 位置座標の記録仕様

## 18.1 記録間隔

プレイヤーの位置座標は、**0.1秒ごと**に記録する。

1秒間に10件の位置データを記録することで、プレイヤーの移動経路、方向転換、停止、移動距離などを比較的細かく分析できるようにする。

毎フレーム記録すると、端末のフレームレートによって記録件数が変化し、データ量も過剰になる可能性がある。

そのため、フレームレートとは独立して、100ミリ秒ごとの一定間隔で記録する。

```javascript
// 端末のフレームレートに関係なく同じ条件で行動データを比較できるように、
// プレイヤーの位置は100ミリ秒ごとの固定間隔で記録する。
const MOVEMENT_LOG_INTERVAL_MS = 100;
```

---

## 18.2 記録する座標

分析用の移動ログには、プレイヤーの以下の座標を記録する。

- X座標
- Y座標

本プロジェクトの分析では、迷路を上から見た平面上の移動を扱う。

ただし、A-Frameの3D空間では、一般的に以下の座標軸が使用される。

- A-FrameのX座標：左右方向
- A-FrameのY座標：高さ方向
- A-FrameのZ座標：前後方向

そのため、本プロジェクトでは、分析用CSVの座標を以下のように定義する。

| 分析用CSV | A-Frame内部 | 意味 |
|---|---|---|
| `pos_x` | `position.x` | 迷路上の左右方向 |
| `pos_y` | `position.z` | 迷路上の前後方向 |

A-Frame内部の高さを表す`position.y`は、基本的な移動経路分析には使用しない。

プレイヤーがジャンプや階段を使用しない平面迷路であるため、高さ方向を除外しても移動経路の分析には影響しない。

```javascript
const worldPosition = player.object3D.position;

const movementLog = {
  elapsedTime,
  // 分析では迷路を上から見た平面経路を扱うため、
  // A-FrameのX-Z平面をCSV上のX-Y座標として保存する。
  posX: worldPosition.x,
  posY: worldPosition.z
};
```

---

## 18.3 移動ログの記録項目

0.1秒ごとに、最低限以下の情報を記録する。

- セッションID
- プレイヤーID
- ルームID
- ゲームモード
- ゲーム開始からの経過時間
- X座標
- Y座標
- 移動速度
- プレイヤーの向き
- 現在のエリアID
- 取得済みコイン数
- 残りコイン数
- 現在の順位
- 最も近い他プレイヤーとの距離

競争プレイ以外では、順位と他プレイヤーとの距離を空欄または`NULL`として保存する。

---

## 18.4 movement_logs.csvの仕様

`movement_logs.csv`は、以下の列構成とする。

```csv
session_id,player_id,room_id,game_mode,elapsed_time,pos_x,pos_y,rotation_y,speed,area_id,collected_coins,remaining_coins,nearest_opponent_distance,current_rank
```

各行は、あるプレイヤーについて0.1秒ごとに記録された1地点を表す。

例：

```csv
session_001,player_001,room_001,solo,0.0,1.20,3.40,90.0,0.00,area_01,0,10,,
session_001,player_001,room_001,solo,0.1,1.25,3.40,90.0,0.50,area_01,0,10,,
session_001,player_001,room_001,solo,0.2,1.35,3.40,90.0,1.00,area_01,0,10,,
```

---

## 18.5 SQLite3のmovement_logsテーブル

移動ログ用テーブルは、以下の構成とする。

```sql
CREATE TABLE movement_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id TEXT NOT NULL,
    player_id TEXT NOT NULL,
    room_id TEXT,
    game_mode TEXT NOT NULL,
    elapsed_time REAL NOT NULL,
    pos_x REAL NOT NULL,
    pos_y REAL NOT NULL,
    rotation_y REAL,
    speed REAL,
    area_id TEXT,
    collected_coins INTEGER,
    remaining_coins INTEGER,
    nearest_opponent_distance REAL,
    current_rank INTEGER,
    FOREIGN KEY (session_id) REFERENCES sessions(session_id)
);
```

A-Frame内部の高さ座標は、このテーブルには保存しない。

ただし、将来的に階段、ジャンプ、高低差などを導入する場合は、`pos_height`などの列を追加して別途記録する。

---

## 18.6 移動距離の計算

総移動距離は、0.1秒ごとに記録した連続するX・Y座標間の距離を合計して求める。

2つの記録地点の距離は、以下の式で計算する。

```text
区間距離 = √((x₂ - x₁)² + (y₂ - y₁)²)
```

総移動距離は、すべての区間距離を合計した値とする。

```python
import numpy as np

movement_logs["diff_x"] = (
    movement_logs.groupby("session_id")["pos_x"].diff()
)

movement_logs["diff_y"] = (
    movement_logs.groupby("session_id")["pos_y"].diff()
)

# 0.1秒ごとの座標差から区間距離を求め、
# クリア時間だけでは判断できない経路の効率を分析できるようにする。
movement_logs["segment_distance"] = np.sqrt(
    movement_logs["diff_x"] ** 2
    + movement_logs["diff_y"] ** 2
)

total_distance = (
    movement_logs.groupby("session_id")["segment_distance"]
    .sum()
    .reset_index(name="total_distance")
)
```

---

## 18.7 ログの一括送信

位置は0.1秒ごとに記録するが、0.1秒ごとにExpressサーバーへAPIリクエストを送信してはならない。

クライアント側で位置ログを一時的に保存し、一定件数ごとにまとめて送信する。

初期設定は以下とする。

- 記録間隔：0.1秒
- 1回の送信件数：50件
- 通常時の送信間隔：約5秒

```javascript
const MOVEMENT_LOG_INTERVAL_MS = 100;
const MOVEMENT_LOG_BATCH_SIZE = 50;

const movementLogBuffer = [];

function addMovementLog(log) {
  movementLogBuffer.push(log);

  // 0.1秒ごとにAPI通信するとサーバーとSQLite3への負荷が大きくなるため、
  // 約5秒分のログをまとめて送信する。
  if (movementLogBuffer.length >= MOVEMENT_LOG_BATCH_SIZE) {
    sendMovementLogs();
  }
}
```

ゲーム終了、ページ離脱、通信切断などが発生した場合は、50件に達していなくても未送信ログの送信を試みる。

---

## 18.8 0.1秒記録に関する注意事項

0.1秒間隔では、1人のプレイヤーについて1分間に約600件の移動ログが作成される。

例として、10人がそれぞれ5分間プレイした場合は、次の件数になる。

```text
600件 × 5分 × 10人 = 30,000件
```

そのため、以下を必須とする。

- ログを1件ずつサーバーへ送らない
- SQLite3への保存にはトランザクションを利用する
- 複数ログを一括してINSERTする
- ゲーム終了時に未送信ログを送信する
- 同じログが重複保存されない仕組みを用意する
- 記録時刻またはログ番号の順番を維持する
- CSV出力時には`elapsed_time`順に並べる
- 分析前に重複行と欠損データを確認する

---

## 18.9 コメントルールへの追加

0.1秒ごとの位置記録に関係するコードには、以下の理由が分かるコメントを追加する。

- なぜ0.1秒間隔で記録するのか
- なぜ毎フレーム記録しないのか
- なぜA-FrameのX-Z座標をCSVのX-Y座標として保存するのか
- なぜログを一時的にバッファへ保存するのか
- なぜログをまとめて送信するのか
- なぜゲーム終了時に未送信ログを送るのか
- なぜ総移動距離をX・Y座標から計算するのか

一方、次のような明らかな処理には冗長なコメントを追加しない。

```javascript
movementLogBuffer.push(log);
```

ただし、その行を含む処理全体について、一括送信を採用する理由がコードだけでは分からない場合は、処理の直前に理由を説明するコメントを追加する。

## 18.10 移動経路の可視化を目的とした座標記録

本プロジェクトでは、プレイヤーの足取りを後から視覚的に確認できるようにするため、ゲーム開始から終了までのX・Y座標を**0.1秒ごと**に記録する。

記録した座標を時間順に線で結ぶことで、プレイヤーが迷路内をどのような経路で移動したのかを、上から見た移動経路図として可視化する。

座標を記録する目的は、クリア時間などの最終結果だけでは分からない、以下の行動を確認することである。

- プレイヤーが実際に通った経路
- 同じ場所を繰り返し通った箇所
- 行き止まりへ進入した箇所
- 長時間停止していた可能性のある箇所
- 大きく方向転換した箇所
- コインを取得した順番
- 速いプレイヤーと遅いプレイヤーの経路の違い
- 個人プレイとマルチプレイでの経路の違い
- 繰り返しプレイした際の経路の変化

0.1秒ごとに記録することで、記録間隔が粗すぎる場合に発生する経路の欠落を抑え、方向転換や細かな移動もある程度確認できるようにする。

ただし、描画フレームごとに記録すると端末性能やフレームレートによってデータ数が変化するため、フレームレートには依存せず、100ミリ秒ごとの固定間隔で記録する。

---

### 移動経路の可視化方法

記録した`movement_logs.csv`の座標をpandasで読み込み、matplotlibを使用して移動経路を描画する。

基本的には、以下のように時間順に並べたX・Y座標を線で結ぶ。

```python
target_route = (
    movement_logs[
        movement_logs["session_id"] == target_session_id
    ]
    .sort_values("elapsed_time")
)

# 0.1秒ごとの座標を時間順に結ぶことで、
# プレイヤーが実際に移動した足取りを上から見た経路として可視化する。
plt.plot(
    target_route["pos_x"],
    target_route["pos_y"]
)

plt.xlabel("X position")
plt.ylabel("Y position")
plt.title("Player movement route")
plt.axis("equal")
plt.show()
```

移動経路図では、必要に応じて以下の情報も追加する。

- スタート地点
- ゲーム終了地点
- コインの配置地点
- コインを取得した地点
- 行き止まりの位置
- 停止時間が長かった地点
- 移動方向
- 経過時間
- 他プレイヤーの移動経路

---

### 比較用の経路図

収集した座標データから、最低限以下の経路図を作成する。

1. クリアが速いプレイヤーの経路
2. クリアが遅いプレイヤーの経路
3. 同じプレイヤーの1回目と2回目の経路
4. 個人プレイ時の経路
5. マルチプレイ時の経路

経路図を比較することで、クリア時間の違いが単なる移動速度の差によるものか、経路選択や再訪問の違いによるものかを考察する。

---

### コイン取得地点の表示

コイン取得ログと移動ログを組み合わせ、プレイヤーがどの地点で、どの順番にコインを取得したかを経路図上に表示する。

コイン取得地点には、取得順序を示す番号を付ける。

これにより、以下を確認できるようにする。

- 効率的なコイン取得順序
- 遠回りが発生したコイン間の移動
- プレイヤーごとの探索順序の違い
- マルチプレイによって取得順序が変化したか

---

### 停止地点の可視化

連続する座標の変化が一定値未満である状態が続いた場合、その区間を停止状態として扱う。

停止時間が長かった地点を経路図上に表示することで、プレイヤーが経路選択に迷った可能性のある場所を確認する。

ただし、座標がほとんど変化していないことだけを理由に、プレイヤーが迷っていたと断定してはならない。

停止には、以下の理由も考えられる。

- 周囲を確認していた
- 他プレイヤーを見ていた
- 操作を一時的に中断した
- 通信や画面表示に問題が発生した

そのため、停止地点は客観的な行動データとして示し、心理状態については可能性として考察する。

---

### 複数プレイヤーの足取り

マルチプレイでは、同じルームに参加したプレイヤーごとに移動経路を作成する。

同じ迷路上に複数プレイヤーの経路を表示することで、以下を確認する。

- プレイヤー同士が似た経路を通ったか
- 他プレイヤーを追うような経路が発生したか
- プレイヤー同士が接近した場所
- 個人プレイ時とは異なる経路を選択したか
- 競争中に経路選択が変化した可能性があるか

複数の経路を同じ図へ表示すると見づらくなる場合は、プレイヤーごとに個別の経路図を作成する。

---

### 座標記録に関する確定仕様

- 座標の記録間隔は0.1秒とする
- 1秒間に10件の座標を記録する
- 記録時にはゲーム開始からの経過時間を含める
- 座標はセッションIDおよびプレイヤーIDと関連付ける
- 座標は時系列順に並べられるようにする
- 個人プレイとマルチプレイを区別して記録する
- マルチプレイではルームIDも記録する
- 記録した座標はCSVへ出力する
- pandasで座標データを読み込む
- matplotlibで足取りを移動経路として可視化する
- 経路図を速い人と遅い人、個人プレイとマルチプレイの比較に使用する

---

### AIコーディング支援への追加指示

AIは、位置座標の記録機能を実装する際、以下の仕様を変更または省略してはならない。

- X・Y座標を0.1秒ごとに記録する
- 記録時刻として経過時間を保存する
- 各座標をセッションIDとプレイヤーIDに関連付ける
- CSVへ時系列データとして出力できるようにする
- pandasで読み込める列構成にする
- matplotlibで座標を時間順に結び、足取りを可視化できるようにする
- 位置ログを毎フレーム記録しない
- 0.1秒ごとにサーバー通信を行わず、ログをまとめて送信する
- ゲーム終了時には未送信の座標ログを送信する

また、関連するコードには、次の設計理由が分かるコメントを追加する。

- なぜ0.1秒ごとに座標を記録するのか
- なぜフレームごとに記録しないのか
- なぜ経過時間と座標を一緒に記録するのか
- なぜ位置ログをまとめて送信するのか
- なぜ座標を時系列順に並べる必要があるのか
- なぜ座標を線で結んで可視化するのか

コードを見れば明らかな単純な代入や配列への追加には、冗長性を防ぐため不要なコメントを追加しない。

---

# 19. コイン取得時の視覚エフェクト

プレイヤーがコインを取得したことを分かりやすくするため、コイン取得時に画面右上のステータスバーを一瞬光らせる視覚エフェクトを実装する。

コインが消えるだけでは、視点の向きや移動速度によって取得できたか判断しにくい場合がある。

そのため、コイン取得時には以下を同時に行う。

- 取得したコインを3D空間から非表示または削除する
- 取得済みコイン数を更新する
- 残りコイン数を更新する
- 画面右上のステータスバーを一瞬光らせる
- コイン取得ログを記録する
- マルチプレイでは取得状況をSocket.IOサーバーへ送信する

---

## 19.1 右上ステータスバー

ゲーム画面の右上には、現在の進行状況を表示するステータスバーを配置する。

最低限、以下の情報を表示する。

- 取得済みコイン数
- 総コイン数
- 残りコイン数
- 経過時間

表示例：

```text
COINS 5 / 10
REMAINING 5
TIME 00:32.4
```

ステータスバーはA-Frame空間内へ固定するのではなく、通常のHTML要素として画面右上へ重ねて表示する。

これにより、プレイヤーがカメラの向きを変更しても常に情報を確認できるようにする。

---

## 19.2 コイン取得時の発光エフェクト

コインを取得した瞬間、右上のステータスバーへ一時的な発光用CSSクラスを追加する。

発光は短時間だけ表示し、その後は通常表示へ戻す。

初期設定は以下とする。

- 発光時間：300ミリ秒程度
- 発光開始：コイン取得と同時
- 発光終了：発光用CSSクラスを削除
- 次のコインを連続して取得した場合：エフェクトを最初から再実行

発光時間は、予備テストを行った上で調整できる設定値として管理する。

```javascript
// コイン取得を視覚的に認識しやすくするため、
// 右上のステータスバーへ短時間だけ発光用クラスを追加する。
function flashCoinStatusBar() {
  const statusBar = document.getElementById("coin-status-bar");

  if (!statusBar) {
    return;
  }

  // 短時間に連続してコインを取得した場合も毎回エフェクトを再生できるように、
  // 一度クラスを削除してから再追加する。
  statusBar.classList.remove("coin-collected-flash");

  requestAnimationFrame(() => {
    statusBar.classList.add("coin-collected-flash");
  });
}
```

CSSアニメーションの終了時に、発光用クラスを削除する。

```javascript
const statusBar = document.getElementById("coin-status-bar");

statusBar?.addEventListener("animationend", () => {
  statusBar.classList.remove("coin-collected-flash");
});
```

---

## 19.3 CSSアニメーション

発光エフェクトは、CSSアニメーションを使用して実装する。

```css
#coin-status-bar {
  position: fixed;
  top: 16px;
  right: 16px;
  z-index: 1000;
}

.coin-collected-flash {
  animation: coin-status-flash 300ms ease-out;
}

@keyframes coin-status-flash {
  0% {
    filter: brightness(1);
    transform: scale(1);
  }

  40% {
    filter: brightness(1.8);
    transform: scale(1.05);
  }

  100% {
    filter: brightness(1);
    transform: scale(1);
  }
}
```

色や発光の強さは、ゲーム全体のデザインに合わせて調整してよい。

ただし、画面全体が大きく点滅するような強すぎる表現は避ける。

---

## 19.4 コイン取得処理との連携

コイン取得時は、以下の順番を基本とする。

1. 取得可能なコインであることを確認する
2. 二重取得でないことを確認する
3. コインを取得済み状態へ変更する
4. 取得コイン数を更新する
5. ステータスバーの表示を更新する
6. ステータスバーの発光エフェクトを実行する
7. コイン取得ログを保存する
8. マルチプレイの場合はサーバーへ取得情報を送信する
9. すべてのコインを取得したか確認する
10. 条件を満たしていればクリア処理を実行する

```javascript
function handleCoinCollected(coin) {
  if (coin.dataset.collected === "true") {
    return;
  }

  coin.dataset.collected = "true";
  coin.setAttribute("visible", false);

  collectedCoins += 1;
  remainingCoins -= 1;

  updateCoinStatusBar();
  flashCoinStatusBar();
  recordCoinLog(coin);

  if (gameMode === "multiplayer") {
    sendCoinCollectedEvent(coin);
  }

  checkGameCleared();
}
```

---

## 19.5 二重取得の防止

同じコインについて、接触判定が短時間に複数回発生する可能性がある。

そのため、コイン取得処理の最初に取得済み状態を確認し、同じコインについて以下が複数回実行されないようにする。

- 取得数の加算
- 発光エフェクト
- コイン取得ログの保存
- Socket.IOイベントの送信

発光エフェクトが複数回発生した場合は、視覚的に不自然になるだけでなく、取得数やログも重複する可能性があるため、取得判定とエフェクト処理を同じ取得処理内で管理する。

---

## 19.6 マルチプレイでの扱い

マルチプレイでも、ステータスバーの発光エフェクトは、自分がコインを取得したときだけ実行する。

他プレイヤーがコインを取得した場合、自分のステータスバーは発光させない。

本プロジェクトでは各プレイヤーが独立したコイン取得状態を持つため、自分の取得状況と他プレイヤーの取得状況を混同しないようにする。

他プレイヤーの取得状況は、順位や取得コイン数の表示だけを更新する。

---

## 19.7 行動ログへの影響

ステータスバーの発光は、プレイヤーに取得結果を伝えるための画面表示であり、新しい分析指標としては扱わない。

コイン取得の分析には、既存のコイン取得ログを使用する。

最低限、以下を記録する。

- セッションID
- プレイヤーID
- ルームID
- コインID
- 取得時刻
- ゲーム開始からの経過時間
- 取得順序
- コインのX・Y座標
- 取得時点の取得済みコイン数
- 取得時点の残りコイン数
- マルチプレイ時の順位

エフェクトの実行有無を毎回CSVへ記録する必要はない。

ただし、エフェクト処理中にエラーが発生しても、コイン取得ログの保存処理が停止しないようにする。

---

## 19.8 アクセシビリティへの配慮

視覚エフェクトは、短時間かつ画面の一部分だけで実行する。

強い点滅や、画面全体の急激な明暗変化は使用しない。

ユーザーの端末で動きの軽減設定が有効になっている場合は、拡大アニメーションを省略し、明るさの変化だけにする。

```css
@media (prefers-reduced-motion: reduce) {
  .coin-collected-flash {
    animation-duration: 150ms;
  }

  @keyframes coin-status-flash {
    0% {
      filter: brightness(1);
    }

    50% {
      filter: brightness(1.5);
    }

    100% {
      filter: brightness(1);
    }
  }
}
```

---

## 19.9 受け入れ条件

以下をすべて満たした場合、コイン取得エフェクトが完成したと判断する。

- コインを取得すると取得数が正しく更新される
- コインを取得すると残りコイン数が正しく更新される
- コイン取得時に右上のステータスバーが一瞬光る
- 発光後は通常の表示へ戻る
- 連続してコインを取得した場合も、その都度発光する
- 他プレイヤーの取得によって自分のステータスバーが発光しない
- 同じコインで取得処理やエフェクトが重複しない
- エフェクトが失敗してもコイン取得ログは保存される
- 発光によってゲーム操作が停止しない
- 発光によって0.1秒ごとの座標記録が停止しない
- ソロプレイとマルチプレイの両方で動作する
- ゲームを連続して開始した場合も正常に動作する

---

## 19.10 AIコーディング支援への追加指示

AIは、コイン取得処理を実装または修正する場合、以下の仕様を変更または省略してはならない。

- コイン取得時に右上のステータスバーを一瞬光らせる
- ステータスバーはHTML要素として画面右上へ固定する
- 発光はCSSアニメーションで実装する
- 発光時間は設定値として変更できるようにする
- 連続取得時にも毎回エフェクトを再生する
- 二重取得を防止する
- 他プレイヤーの取得では自分のステータスバーを発光させない
- エフェクト処理とコイン取得ログの保存を混同しない
- エフェクトの不具合によってゲーム進行やログ保存を停止させない
- 強すぎる点滅や画面全体の点滅を使用しない

関連するコードには、以下の設計理由が分かるコメントを追加する。

- なぜコイン取得時に視覚エフェクトを表示するのか
- なぜ右上のステータスバーを発光させるのか
- なぜ発光用クラスを一度削除してから再追加するのか
- なぜ同じコインの二重取得を防止するのか
- なぜ他プレイヤーの取得時には発光させないのか
- なぜエフェクトとログ保存を分離するのか

必要なコメントの例：

```javascript
// コインが消えるだけでは取得できたことを認識しにくいため、
// 常に視界へ入る右上のステータスバーを短時間発光させる。
flashCoinStatusBar();
```

不要なコメントの例：

```javascript
// ステータスバーを取得する
const statusBar = document.getElementById("coin-status-bar");
```

コードから処理内容が明らかな部分には、冗長性防止のためコメントを追加しない。

---

# 20. ステージ追加・参加コード・CSV出力制限

既存ステージを標準のデータ収集に使用する`basic`として維持し、形状とコイン数が異なる4種類のステージを追加する。

| ステージID | 用途 | コイン数 | 参加条件 |
|---|---|---:|---|
| `basic` | 標準データ収集 | 8 | コード不要 |
| `forest` | 分岐の多い探索 | 6 | 専用コードが必要 |
| `crossroads` | 中央分岐型の探索 | 10 | 専用コードが必要 |
| `spiral` | 回り込みを含む探索 | 7 | 専用コードが必要 |
| `grid` | 経路選択肢が多い探索 | 12 | 専用コードが必要 |

迷路配置、コイン配置、コイン数、開始位置などのステージ固有情報は一つの設定ファイルで管理し、複数箇所へ同じ値を記述しない。

## 20.1 ベーシックステージ

参加コードが空欄の場合は`basic`を選択する。既存データとの比較可能性を維持するため、現在の迷路形状、開始位置、コイン8個の配置は変更しない。

## 20.2 追加ステージへの参加

追加ステージは、入力画面で専用コードを入力し、サーバーの検証に成功した場合だけ開始できる。

参加コードの正解をHTMLやクライアントJavaScriptへ記述してはならない。サーバーはコード検証に成功した場合だけ、ステージID、表示名、迷路配置、コイン配置、総コイン数、開始位置を返す。不正なコードの場合はゲームを開始せず、入力画面へエラーを表示する。

参加コードは環境変数で上書き可能にする。開発用コードを用意する場合もゲーム画面には表示しない。

## 20.3 ステージとログの関係

- セッションの`maze_id`には実際に使用したステージIDを保存する
- `total_coins`、HUD、クリア判定、移動ログとコインログの`remaining_coins`は選択ステージのコイン数から計算する
- ソロの連続プレイでは最初に選択したステージを引き継ぐ
- マルチプレイでは同じルームの全プレイヤーが同じステージを使用する
- 異なるステージで既存ルームへ参加しようとした場合は参加を拒否する
- 次ラウンドでも同じステージ設定を維持する

## 20.4 CSV出力の制限

ステージクリア後の結果画面にはCSV出力リンクを表示しない。

CSV出力APIは研究管理者用トークンを提示した場合だけ利用可能にし、トークンがない場合または一致しない場合は`401 Unauthorized`を返す。

研究管理者用トークンはクライアントHTMLやゲームJavaScriptへ埋め込まず、サーバー環境変数または起動時に生成した値として管理する。一般プレイヤーは結果を閲覧できるが、生の実験データは取得できない構成とする。

## 20.5 保守性に関する必須方針

- 先に`context.md`へ仕様を確定し、その後に実装する
- 既存機能を置き換えず、受け入れ条件に必要な範囲だけ変更する
- ステージ定義を一つの設定ファイルへ集約する
- ステージ固有の条件分岐やコイン数を複数ファイルへ重複させない
- 既存のログ、連続プレイ、Socket.IOラウンド管理を維持する
- 関数名と定数名から目的が分かる構成にする
- 単純な処理を言い換える冗長なコメントは追加しない
- 設計理由やセキュリティ上の意図だけをコメントで補足する

## 20.6 受け入れ条件

- コードなしで既存の`basic`を開始できる
- 4種類の追加ステージが存在する
- 追加ステージは正しいコードを入力した場合だけ開始できる
- 不正なコードではゲームを開始できない
- ステージごとにコイン数が異なる
- HUD、クリア判定、ログの総数と残数が選択ステージに一致する
- 連続プレイとマルチプレイの次ラウンドでも同じステージを維持する
- 同じマルチプレイルームで異なるステージが混在しない
- 結果画面にCSV出力リンクが存在しない
- 認証トークンなしではCSV出力APIを利用できない
- 管理者トークンを提示した場合は従来どおりCSVを出力できる
- ステージ設定やコイン数が重複定義されていない

## 20.7 AIコーディング支援への追加指示

AIは、本項を実装する場合、必ず`context.md`への仕様追加を完了してから機能コードを変更する。実装範囲は必要最小限とし、既存コードを大規模に再構成しない。

追加ステージ、参加コード、CSV認証に関係するコードには、ステージ設定を一か所へ集約する理由、参加コードをサーバーで検証する理由、同じルームでステージを統一する理由、CSVトークンをブラウザへ渡さない理由がコードだけでは分かりにくい場合に限りコメントを追加する。

---

# 21. Railway本番運用

既存のA-Frame、WebXR、Express、Socket.IO、SQLite3、連続ラウンド、0.1秒間隔の移動ログ一括保存、コイン・イベントログ、認証付きCSV出力を維持したまま、Railway上で常時稼働できるようにする。

## 21.1 HTTPサーバー

- Railwayが設定する`PORT`を優先し、未設定時はローカル用の3000番を使用する
- `0.0.0.0`で待ち受ける
- ExpressとSocket.IOは一つのHTTPサーバーを共有する
- HTML、ローカル配信のA-Frame、API、Socket.IOを同じオリジンから提供する
- Socket.IOクライアントは`io()`による同一オリジン接続を維持し、本番URLやlocalhostを固定しない
- 同一オリジン構成に不要なCORS許可を追加しない

## 21.2 永続化

RailwayではVolumeを`/data`へマウントし、`DATABASE_PATH=/data/game_data.db`を設定する。`DATABASE_PATH`未設定のローカル環境では従来の`data/game.db`を使用する。

DB保存先の親ディレクトリがない場合は起動時に安全に作成する。既存DBには不足したschemaだけを追加し、起動や再デプロイを理由にテーブルや実験データを削除しない。

本番環境でも`players`、`sessions`、`movement_logs`、`coin_logs`、`event_logs`を同じSQLite DBへ保存する。`players`は初回セッション開始時に`player_id`と作成日時を登録し、同じIDの再利用では重複行を作らない。

SQLite3をデータの正本とする。認証済みCSV出力時に作るスナップショットはDBと同じ永続ディレクトリ配下の`exports/`へ保存し、通常のログ保存とCSVへの二重書き込みは行わない。

## 21.3 Railway設定

- GitHubリポジトリから一つのServiceとしてデプロイする
- VolumeのMount Pathを`/data`にする
- `NODE_ENV=production`と`DATABASE_PATH=/data/game_data.db`をVariablesへ設定する
- `PORT`はRailwayの自動設定を使用する
- `MD2_EXPORT_TOKEN`などの秘密情報はRailway Variablesで管理し、ソースやGitHubへ記録しない
- Public NetworkingでHTTPSの公開URLを生成する
- WebXR表示、別端末からのアクセス、同一ルーム参加、ログ保存、再起動後のデータ維持を確認する

## 21.4 運用制約

Socket.IOのルーム状態はNode.jsのメモリ上にあるため、Railwayでは1インスタンスで運用する。今回はRedis Adapterを追加しない。将来水平スケールする場合は、Redis Adapterなどによるルーム状態共有とSQLite以外の共有DBを別途検討する。

DBファイル、`.env`、依存パッケージ、生成CSVはGitHubへコミットしない。Railway対応のために既存のゲーム仕様、ログ列、バッチ保存、分析形式を削除または簡略化しない。
